# Local working clone of openai/codex used to author and verify the patch.
# Kept on disk so the patch can be regenerated, but not committed.
# The canonical state is branch `fix/subagent-retention-after-19753` off
# upstream `origin/main`, with a single commit producing the patch in
# `patches/pr1-subagent-retention-after-19753.patch`.
codex/

# Raw investigation scratch (large build/test logs, fleet helpers, redundant PR drafts).
# Curated outputs live under artifacts/ and patches/ instead.
codex-investigation-artifacts/

# OS / editor noise
.DS_Store
*.swp
.idea/
.vscode/
